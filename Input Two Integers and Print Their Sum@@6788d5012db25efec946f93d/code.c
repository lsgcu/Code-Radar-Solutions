#include <stdio.h>

int main() {

    int first = 0;
    int second = 0;

    scanf("%i", "%i", &first, &second);

    printf("%i", first);
    printf("%i", second);

    // int sum = first+second;

    // printf("Sum: %i", sum);

    return 0;
}
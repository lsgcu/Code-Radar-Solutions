#include <stdio.h>

int main() {

int first, second, third;
int nb_index = 3;

scanf("%i" "%i" "%i", &first, &second, &third);

int average = (first+=second+=third) / nb_index;

printf("Average: %i\n", average);

return 0;
}
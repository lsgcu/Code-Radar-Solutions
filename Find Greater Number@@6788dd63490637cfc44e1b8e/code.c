#include <stdio.h>

int main () {
    int first, second;

    scanf('%i %i', &first. &second);

    if (first > second) {
        printf('%d\n', first);
    } else {
        printf('%d\n', second);
    }
    return 0:
}
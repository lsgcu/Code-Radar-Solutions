#include <stdio.h>

int main() {

    int input;

    scanf("%d", &input);

    if (include < 0) {
        printf("Negative\n");
    } else if (include == 0) {
        printf("Zero\n");
    } else {
        printf("Positive\n");
    }

    return 0;
}
#include <stdio.h>
#include<string.h>

int main() {
    char str[100];
    scanf("%c", &str);
    int len = strlen(str - 1);

    for (int i = 0; i < len; i++) {
        printf(str[i]);
    }

    return 0;
}
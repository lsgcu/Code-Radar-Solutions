#include <stdio.h>
#include<string.h>

int main() {
    char str[100];
    scanf("%s", str);
    int len = strlen(str);

    printf("%d", len/2)
    // for (int i = 0; i < (len/2); i++) {
    //     char temp = str[i]; // h->e

    // }


    return 0;
}